{% extends 'admin/base_admin.html.twig' %}

{% block content %}    
    <div class="container-fluid">
        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ produit.id }}</td>
                </tr>
                <tr>
                    <th>Nom_pdt</th>
                    <td>{{ produit.nomPdt }}</td>
                </tr>
                <tr>
                    <th>Prix_unitaire</th>
                    <td>{{ produit.prixUnitaire }}</td>
                </tr>
            </tbody>
        </table>
    
        <a href="{{ path('adminproduit') }}">{{'back to list'|trans}}</a>
    
        <a href="{{ path('adminproduit_edit', {'id': produit.id}) }}">{{'edit'|trans}}</a>
    
        {{ include('admin/produit/_delete_form.html.twig') }}
    </div>
    
{% endblock %}
