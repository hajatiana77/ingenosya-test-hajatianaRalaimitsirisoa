{% extends 'admin/base_admin.html.twig' %}

{% block content %}
    <div class="container-fluid">
        <h1>{{'Create new Produit'|trans}}</h1>
    
        {{ include('admin/produit/_form.html.twig') }}
    
        <a href="{{ path('adminproduit') }}">{{'back to list'|trans}}</a>
    </div>
{% endblock %}
