{% extends 'admin/base_admin.html.twig' %}

{% block content %}
<div class="container-fluid">
    <h1>{{'Create new Pointdevente'|trans}}</h1>

    {{ include('admin/pointdevente/_form.html.twig') }}

    <a href="{{ path('adminpointdevente') }}">{{'back to list'|trans}}</a>
</div>
{% endblock %}
