{% extends 'admin/base_admin.html.twig' %}

{% block content %}
<div class="container-fluid">
    {{ include('admin/pointdevente/_form.html.twig', {'button_label': 'Update'|trans}) }}
    
    <a href="{{ path('adminpointdevente') }}">{{'back to list'|trans}}</a>
    
    {{ include('admin/pointdevente/_delete_form.html.twig') }}
</div>
{% endblock %}
